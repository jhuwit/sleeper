% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/classify.R
\name{sl_python_modules_installed}
\alias{sl_python_modules_installed}
\alias{estimate_sleep}
\title{Estimate Sleep from Wrist-Worn Accelerometry}
\usage{
sl_python_modules_installed(component = c("models", "features"))

estimate_sleep(data, epoch = 30L, model_dir, cores = 1L)
}
\arguments{
\item{component}{Python dependency set to check. The default, \code{"models"},
checks all modules needed for sleep classification; \code{"features"} checks
only the feature-extraction modules.}

\item{data}{A \code{data.frame} with columns of \code{timestamp}, \code{x}, \code{y}, \code{z}}

\item{epoch}{Time in seconds for the time interval estimate}

\item{model_dir}{path to the folder with models from
\doi{10.5281/zenodo.3752645}}

\item{cores}{Number of workers to use for model prediction. Any non-zero
integer accepted by Python \code{joblib} is allowed; \code{-1} uses all available
workers. Defaults to one worker for CRAN compatibility.}
}
\value{
A \code{tibble} of times and classification of category.
}
\description{
Estimate Sleep from Wrist-Worn Accelerometry
}
\examples{
\donttest{
  if (sl_python_modules_installed("models")) {
    models = sl_example_model()
    if (!is.null(models) && requireNamespace("readr", quietly = TRUE)) {
      file = system.file("extdata", "example_data.csv.gz", package = "sleeper")
      data = readr::read_csv(file, n_max = 3600L)
      # Two minutes is sufficient to demonstrate the workflow and avoids
      # applying the forest to the full 12-hour example recording.
      try({estimate_sleep(data, model_dir = dirname(models$binary[1]))})
    }
  }
}
}
